# 教程四：回收区

**本教程目标**：创建一个资源回收区域，玩家走进后头顶的物品自动卸载存储，并预留类型过滤接口。

完成后你可以：
- 场景里有一个回收区标记
- 头顶有物品的玩家走进回收区 → 物品清空 → 存储计数增加
- 控制台输出回收日志
- 类型过滤接口已预留，后续可直接扩展

---

## 第一部分：思路引导

### 回收区要检测什么？

回顾之前的模式：

| 组件 | 检测方式 | 触发动作 |
|------|---------|---------|
| ResourceNode | 玩家 WeaponRange 检测 ResourceNode | 玩家开始攻击 |
| DroppedItem | 玩家 PickupRange 检测 DroppedItem | 物品飞向玩家 |
| 回收区 | **回收区**检测玩家 body_entered | 吸走玩家头顶物品 |

回收区自己就是 Area2D，检测玩家进入。玩家走进来 → 回收区调用 `player.remove_all_from_head()` → 把得到的物品数组存起来。

### 存储结构用什么？

后续会区分不同资源类型放入不同回收区。所以存储结构要支持"按类型查询"：

```gdscript
var stored: Dictionary = {
    "矿石": 5,
    "木材": 3,   # 未来
}
```

这种结构天然支持按类型过滤。现在所有物品都是"矿石"，但预留了 `_can_accept(item_type)` 方法。

### 预留接口设计

```gdscript
func _can_accept(item_type: ItemData.ItemType) -> bool:
    # 目前接受所有 RESOURCE 类型
    # 后续子类覆盖此方法即可实现类型过滤
    return item_type == ItemData.ItemType.RESOURCE
```

---

## 第二部分：完整操作步骤

### 步骤 1：创建 CollectionZone 脚本

新建 `scripts/collection_zone.gd`：

```gdscript
class_name CollectionZone
extends Area2D

signal resources_deposited(item_count: int)

@export var filter_enabled: bool = false    # 开启后只接受特定类型
@export var filter_type: int = ItemData.ItemType.RESOURCE

var stored: Dictionary = {}
var _deposit_player: Node2D = null  # 正在上缴的玩家引用
var _deposit_timer: float = 0.0     # 上缴间隔计时器
var _is_depositing: bool = false    # 是否正在上缴中
var _stored_count: int = 0          # 本次上缴的物品计数

func _ready() -> void:
	# 占位标记贴图
	var img := Image.create(64, 64, false, Image.FORMAT_RGBA8)
	img.fill(Color(1.0, 0.85, 0.0, 0.3))
	$Sprite2D.texture = ImageTexture.create_from_image(img)
	
	body_entered.connect(_on_body_entered)

func _process(delta: float) -> void:
	if not _is_depositing:
		return
	_deposit_timer += delta
	if _deposit_timer >= 0.15:
		_deposit_timer = 0.0
		_deposit_next()

func _on_body_entered(body: Node2D) -> void:
	if not body.has_method("pop_from_head"):
		return
	if not body.has_method("can_pickup"):
		return
	if body.head_stack.size() <= 0:
		return
	if _is_depositing:
		return
	
	_deposit_player = body
	_is_depositing = true
	_deposit_timer = 0.0

func _deposit_next() -> void:
	var data = _deposit_player.pop_from_head()
	if data == null:
		_is_depositing = false
		if _stored_count > 0:
			resources_deposited.emit(_stored_count)
			_print_stored()
		return
	
	# 创建飞行精灵，从头顶抛物线飞到回收区
	var fly_item := Sprite2D.new()
	fly_item.texture = data.icon if data and data.icon else null
	fly_item.scale = Vector2(0.4, 0.4)
	var start_pos := _deposit_player.global_position + Vector2(0, -40)
	fly_item.global_position = start_pos
	get_tree().current_scene.add_child(fly_item)
	
	var tween := create_tween()
	tween.tween_method(func(t: float):
		var pos := start_pos.lerp(global_position, t)
		pos.y -= sin(t * PI) * 60
		fly_item.global_position = pos
	, 0.0, 1.0, 0.4)
	tween.tween_callback(func():
		fly_item.queue_free()
		if not _can_accept(data.item_type):
			return
		var type_name := data.item_name
		stored[type_name] = stored.get(type_name, 0) + 1
		_stored_count += 1
	)

func _can_accept(item_type: ItemData.ItemType) -> bool:
	if not filter_enabled:
		return true
	return item_type == filter_type

func _print_stored() -> void:
	print("=== 回收区存储 ===")
	for type_name in stored:
		print("  %s: %d 个" % [type_name, stored[type_name]])
	print("=================")
```

### 步骤 1b：在 player.gd 末尾追加 pop_from_head 方法

回收区需要一个一个地从玩家头顶弹出物品，在 `scripts/player.gd` 的 `remove_all_from_head` 旁边追加：

```gdscript
# 从头顶弹出最上面一个物品（用于逐个飞向回收区）
func pop_from_head() -> ItemData:
	if head_stack.is_empty():
		return null
	var data = head_stack.pop_back()
	_update_head_display()
	return data
```

### 步骤 2：创建回收区场景

新建 `scenes/collection_zone.tscn`：

```
CollectionZone (Area2D)           ← 挂载 collection_zone.gd 脚本
├── CollisionShape2D              ← RectangleShape2D, size=(128, 128)
└── Sprite2D                      ← 回收区标记
```

**操作**：
1. 新建场景 → 根节点 Area2D，命名为 CollectionZone
2. 挂载脚本 `res://scripts/collection_zone.gd`
3. 添加子节点 CollisionShape2D，shape 选 RectangleShape2D，size=(128, 128)
4. 添加子节点 Sprite2D，用于视觉标记
5. 在组（Groups）中添加 `"collection_zone"`
6. 标记贴图已经在脚本的 `_ready()` 里生成，不需要额外操作

### 步骤 3：放置到主场景

打开 `scenes/main.tscn`，把 `collection_zone.tscn` 拖入场景，放在玩家出生点附近（比如 (640, 300)）。

---

## 验证

1. 运行游戏，先打矿石、拾取头顶几个矿石
2. 走到回收区（金色方块标记）
3. 头顶物品逐个飞向回收区（抛物线轨迹，每个间隔 0.15 秒）
4. 控制台输出：

```
=== 回收区存储 ===
  矿石: 3 个
=================
```

5. 再次打矿拾取，再次走进回收区 → 数量累加，同样逐个抛物线上缴

---

## 完整链路回顾

至此，整个资源采集链路完成：

```
发现矿石 → 自动挥砍破坏 → 粒子爆射 + 物品弹落
       ↓
拾取圈吸附 → 飞向玩家 → 堆叠头顶
       ↓
走进回收区 → 逐个抛物线上缴 → 存储计数
```

你已经实现的 4 个系统：

| 教程 | 新增文件 | 修改文件 |
|------|---------|---------|
| 一 | ore_node.gd, ore_node.tscn, ore_item.tres | item_data.gd |
| 二 | — | player.tscn, player.gd, ore_node.gd |
| 三 | dropped_item.gd, dropped_item.tscn | player.gd, player.tscn, ore_node.gd |
| 四 | collection_zone.gd, collection_zone.tscn | main.tscn |

## 后续预留扩展点

| 扩展方向 | 涉及文件 | 预留接口 |
|---------|---------|---------|
| 不同资源类型（木材、石材） | 新建 ItemData .tres | ItemType 枚举 |
| 按类型过滤回收区 | collection_zone.gd | `@export filter_type`，检查器直接配置 |
| 堆叠上限升级 | player.gd | `max_head_stack` 变量 |
| 矿石多阶段破坏 | ore_node.gd | `_die()` 可覆盖 |
| 资源解锁系统 | 新建 Manager | stored 数据 |
