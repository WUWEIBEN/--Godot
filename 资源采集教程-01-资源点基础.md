# 教程一：资源点基础

**本教程目标**：创建第一个矿石资源点，它能站在场景里，检测到玩家靠近。

完成后你可以：
- 在场景里看到矿石
- 矿石有 HP 属性，能检测到玩家进入/离开

---

## 第一部分：思路引导

### 你应该选什么节点类型？

矿石是"站在场景里不动、玩家可以碰到它"的物体。Godot 里表示静态物理体的节点是哪个？

<details>
<summary>想一想，再展开答案</summary>

**StaticBody2D** — 它不会移动，但参与物理碰撞，玩家撞到它会停下来。

</details>

### 矿石需要哪些子节点？

列一下矿石作为一个"可破坏的资源点"需要什么：

| 功能 | 用什么节点 | 为什么 |
|------|-----------|--------|
| 外观 | ? | 显示矿石的样子 |
| 物理碰撞 | ? | 玩家不能穿过去 |
| 检测玩家靠近 | ? | 需要知道玩家是否在附近 |
| 破坏时粒子特效 | ? | 碎石飞溅 |

<details>
<summary>想一想，再展开答案</summary>

| 功能 | 节点 | 
|------|------|
| 外观 | **Sprite2D** |
| 物理碰撞 | **CollisionShape2D**（配 RectangleShape2D） |
| 检测玩家靠近 | **Area2D** + CollisionShape2D（配 CircleShape2D） |
| 破坏时粒子特效 | **GPUParticles2D** |

Area2D 的检测圈应该比物理碰撞大一圈——玩家不用紧贴着矿石才能被检测到。

</details>

### ItemType 需要扩展吗？

目前的物品类型有 `WEAPON`、`ARMOR`、`BATTERY`、`CONSUMABLE`。矿石掉落的"矿石"物品属于哪种？都不太对。你需要新增一个类型。

---

### 新概念：GPUParticles2D

这是你第一次接触粒子系统。先了解几个关键属性：

| 属性 | 含义 | 本次设置 |
|------|------|---------|
| `one_shot` | true = 发射一次就停，false = 持续发射 | true |
| `emitting` | 当前是否在发射 | false（代码触发） |
| `amount` | 粒子数量 | 20 |
| `lifetime` | 每个粒子存活时间（秒） | 0.6 |
| `process_material` | 控制粒子运动（速度/方向/重力） | 代码创建 |

**粒子是怎么显示的？** GPUParticles2D 需要 `draw_pass_1` 来指定每个粒子长什么样。默认用 QuadMesh，显示为小方块——正好适合像素风格碎石。

---

## 第二部分：完整操作步骤

### 步骤 1：新增 RESOURCE 物品类型

打开 `scripts/item_data.gd`，在枚举里加一个 `RESOURCE`：

```gdscript
enum ItemType { WEAPON, ARMOR, BATTERY, CONSUMABLE, RESOURCE }
```

### 步骤 2：创建矿石物品数据

在 `resource/items/` 文件夹下新建 `ore_item.tres`（在编辑器中右键 → 新建资源 → 搜索 ItemData）：

| 属性 | 值 |
|------|-----|
| 脚本 | `res://scripts/item_data.gd` |
| item_name | "矿石" |
| item_type | RESOURCE |
| icon | 暂时留空（后面补充） |

> 如果 `resource/items/` 目录不存在，先在文件系统中创建它。

### 步骤 3：创建 ResourceNode 基类脚本

这个脚本是所有资源点的通用基类。矿石、树、石头……都共用它，靠检查器里的导出变量区分不同的资源。

新建 `scripts/resource_node.gd`：

```gdscript
class_name ResourceNode
extends StaticBody2D

@export var hp: int = 5
@export var max_hp: int = 5
@export var drop_count: int = 3
@export var drop_item_data: ItemData
@export var texture_color: Color = Color(0.55, 0.35, 0.15)   # 外观颜色
@export var particle_color: Color = Color(0.55, 0.35, 0.15)   # 粒子颜色

var _current_hp: int

func _ready() -> void:
	_current_hp = max_hp
	
	# 根据 texture_color 生成占位贴图
	var size := Vector2(32, 32)
	if $Sprite2D.texture == null:
		var img := Image.create(int(size.x), int(size.y), false, Image.FORMAT_RGBA8)
		img.fill(texture_color)
		$Sprite2D.texture = ImageTexture.create_from_image(img)
	
	_particle_setup()

func _particle_setup() -> void:

	var mat := ParticleProcessMaterial.new()                              # 创建粒子材质（控制粒子怎么运动的"配方"）
	mat.emission_shape = ParticleProcessMaterial.EMISSION_SHAPE_SPHERE    # 粒子出生位置：球体内任意一点，不是从一个点喷出
	mat.emission_sphere_radius = 8.0                                               # 球体半径 8 像素，粒子从矿石中心周围一小块区域出现
	mat.direction = Vector3(0, -1, 0)                                     # 初始飞行方向（向上），spread 会让它散开而不是全往上
	mat.spread = 180.0                                                    # 扩散角度 180° = 朝所有方向飞（360° 的一半）
	mat.initial_velocity_min = 80.0                                       # 最小初速度（像素/秒），有的碎片飞得慢
	mat.initial_velocity_max = 180.0                                      # 最大初速度（像素/秒），有的飞得快，显得随机自然
	mat.gravity = Vector3(0, 200, 0)                                      # 重力（像素/秒²），碎片飞到一半往下掉
	mat.scale_min = 2.0                                                   # 粒子最小尺寸（像素），碎石有小块的
	mat.scale_max = 5.0                                                   # 粒子最大尺寸（像素），碎石有大块的
	mat.color = particle_color                                            # 粒子颜色，跟资源外观颜色一致（矿石用灰棕色）
	mat.lifetime_randomness = 0.5                                         # 存活时间随机度，0.5 意味着有的粒子先消失有的后消失
	$GPUParticles2D.process_material = mat                                # 把配好的材质赋值给 GPUParticles2D 节点

func take_damage(amount: int) -> void:
	_current_hp -= amount
	if _current_hp <= 0:
		_die()

func _die() -> void:
	$GPUParticles2D.emitting = true
	get_tree().create_timer(0.8).timeout.connect(queue_free)
	$Sprite2D.visible = false
	$CollisionShape2D.set_deferred("disabled", true)
```

### 步骤 4：创建矿石场景

新建 `scenes/ore_node.tscn`，按以下结构搭建：

```
OreNode (StaticBody2D)           ← 挂载 resource_node.gd 脚本
├── CollisionShape2D             ← RectangleShape2D, size=(32, 32)
├── Sprite2D                     ← 位置默认即可
├── DetectionArea (Area2D)       ← 放入 "detection" 组（可选）
│   └── CollisionShape2D         ← CircleShape2D, radius≈40
└── GPUParticles2D               ← one_shot=ON, emitting=OFF, amount=20, lifetime=0.6
                                  ← explosiveness=1.0, speed_scale=1.5
```

**操作**：
1. 新建场景 → 根节点选 StaticBody2D，命名为 OreNode
2. 给根节点附加脚本 `res://scripts/resource_node.gd`
3. 添加子节点：CollisionShape2D、Sprite2D、Area2D（命名 DetectionArea）、GPUParticles2D
4. 给 Area2D 添加子节点 CollisionShape2D，shape 选 CircleShape2D
5. 在检查器中设置 GPUParticles2D 属性（`one_shot`、`amount` 等）

**设置检查器参数**：选中 OreNode 节点，检查器中出现 `ResourceNode` 脚本的导出变量：

| 变量 | 值 | 说明 |
|------|-----|------|
| hp | 5 | 生命值 |
| max_hp | 5 | 最大生命值 |
| drop_count | 3 | 掉落物品数量 |
| drop_item_data | `res://resource/items/ore_item.tres` | 掉落什么物品 |
| texture_color | `#8c5926`（棕色） | 矿石外观颜色 |
| particle_color | `#8c5926` | 粒子特效颜色 |

### 步骤 5：放置到主场景

打开 `scenes/main.tscn`，把刚创建的 `ore_node.tscn` 拖入场景，放在玩家出生点附近（比如玩家在 (640, 360)，矿石放 (700, 360)）。

保存所有文件。

---

## 验证

1. 运行游戏 → 看到棕色的矿石方块站在场景里
2. 玩家靠近矿石 → 物理碰撞生效（不会穿过去）
3. 打开编辑器的"调试 → 场景树"面板，确认 OreNode 的 `_ready` 正常执行

> 暂时矿石还不会被破坏——那是下一个教程的内容。
