# 资源采集系统 — 开发计划

## Context

在现有背包/装备/快捷栏系统基础上，新增完整的资源采集链路：
**发现资源 → 自动攻击破坏 → 粒子特效 + 掉落物品 → 人物自动吸附堆叠 → 回收区卸载存储**

用户确认的设计决策：
- 破坏表现：直接消失 + GPUParticles2D 粒子特效（像素风格碎石）
- 堆叠上限：初始 10，可配置（预留升级接口）
- 攻击动画：代码控制 WeaponSprite 旋转挥砍，参考土豆兄弟（sin 波形摆动，不用逐帧动画）
- 回收区形式：简单标记区域（Area2D + 指示精灵）

## 架构总览

```
ResourceNode (StaticBody2D)                  ← 矿石资源点，放入 "resources" 组
├── Sprite2D                            ← 矿石外观
├── CollisionShape2D                    ← 物理碰撞
├── DetectionArea (Area2D)              ← 检测玩家进入
├── GPUParticles2D                      ← 破坏时粒子特效 (one_shot)
└── ore_node.gd
    - hp / max_hp / drop_count
    - drop_item_data: ItemData
    - take_damage(amount) → die → spawn DroppedItems + emit particles + queue_free

DroppedItem (Area2D)                    ← 掉落的资源物品，放入 "dropped_items" 组
├── Sprite2D
├── CollisionShape2D (pickup 检测圈)
└── dropped_item.gd
    - item_data: ItemData
    - scatter(origin, force) → 随机方向弹射
    - attract_to(player) → 飞向玩家
    - picked_up signal

Player (CharacterBody2D)                ← 修改现有
├── ...现有节点...
├── WeaponRange (Area2D)                ← 武器检测范围 (~40px 半径)
├── PickupRange (Area2D)                ← 物品吸附范围 (~80px 半径)
├── HeadStack (Node2D)                  ← 头顶物品挂载点
└── player.gd 修改
    - 自动攻击逻辑：检测目标 → 冷却计时 → 挥砍
    - 武器挥砍动画：sin 波形控制 WeaponSprite.rotation
    - 头顶堆栈：head_stack: Array + max_head_stack: int

CollectionZone (Area2D)                 ← 资源回收区，放入 "collection_zone" 组
├── CollisionShape2D (大矩形)
├── Sprite2D (标记圈)
└── collection_zone.gd
    - stored: Dictionary (type_name → count)
    - _on_body_entered → 吸走玩家头顶所有物品
    - 预留 _can_accept(item_type) 过滤接口
```

## 资源回收链路

```
1. 玩家靠近矿石 → WeaponRange 检测到 ResourceNode（group "resources"）
2. 玩家有武器 → 自动开始攻击
3. WeaponPivot 转向矿石方向 → WeaponSprite sin 波形挥砍
4. 每次挥砍中点造成伤害 → ResourceNode.hp -= attack
5. hp ≤ 0 → 矿石消失 + GPUParticles2D 一次爆发 + 生成 N 个 DroppedItem
6. DroppedItem 随机速度散开 + 摩擦力减速 → 落地静止
7. PickupRange 检测到 DroppedItem → 物品飞向玩家
8. 物品到达 → head_stack 追加（未满时）→ 头顶显示更新
9. 玩家走到 CollectionZone → 物品卸载存储 → head_stack 清空
```

## 新建文件

### 1. `scripts/ore_node.gd` — 矿石资源点脚本
- extends StaticBody2D, class_name ResourceNode
- @export hp, max_hp, drop_count
- @export drop_item_data: ItemData
- DetectionArea body_entered/body_exited → player 引用
- func take_damage(amount)
- func _die() → GPUParticles2D.emitting=true, spawn DroppedItems, queue_free

### 2. `scenes/ore_node.tscn` — 矿石场景
- StaticBody2D根 + ResourceNode脚本
- Sprite2D（先用 ColorRect 占位，后续替换贴图）
- CollisionShape2D + DetectionArea(Area2D + CircleShape2D)
- GPUParticles2D（one_shot=true, emitting=false, amount=20, 小方块纹理）

### 3. `scripts/dropped_item.gd` — 掉落物品脚本
- extends Area2D, class_name DroppedItem
- var item_data: ItemData, _velocity: Vector2, _attracted: bool, _target: Node2D
- func scatter(origin, force) → 随机方向速度
- func attract_to(player) → 设置吸附目标
- _physics_process → 重力+摩擦 / 吸附飞行
- 到达玩家后发出 picked_up 信号并 queue_free

### 4. `scenes/dropped_item.tscn` — 掉落物品场景
- Area2D根 + DroppedItem脚本
- Sprite2D（小尺寸物品贴图）
- CollisionShape2D（拾取检测圈 ~30px）

### 5. `scripts/collection_zone.gd` — 资源回收区脚本
- extends Area2D, class_name CollectionZone
- var stored: Dictionary
- signal resources_deposited(type, count)
- _on_body_entered → 检测 body.has_method("remove_all_from_head") → 转移
- 预留 _can_accept(item_type) → bool

### 6. `scenes/collection_zone.tscn` — 回收区场景
- Area2D根 + CollectionZone脚本 + 大 CollisionShape2D (~128x128)
- Sprite2D（半透明标记圈）

### 7. `resource/items/ore_item.tres` — 矿石 ItemData
- item_name="矿石", item_type=RESOURCE
- icon=矿石贴图（暂用 ColorRect 生成）

## 修改已有文件

### 8. `scripts/item_data.gd`
- ItemType 枚举新增 `RESOURCE` 类型

### 9. `scripts/player.gd`
新增变量：
- `_attack_target: ResourceNode`（当前攻击目标，null=无目标）
- `_attack_cooldown: float`（攻击间隔计时）
- `_is_swinging: bool`（是否正在挥砍）
- `_swing_timer: float`（挥砍动画计时）
- `head_stack: Array`（头顶的物品数据数组）
- `max_head_stack: int = 10`（堆叠上限）
- `stack_display: Array[Sprite2D]`（头顶显示用精灵）

新增方法：
- `_process_attack(delta)` — 攻击冷却 + 触发挥砍
- `_start_swing(target_pos)` — 计算挥砍参数
- `_process_swing(delta)` — sin 波形更新 WeaponSprite.rotation
- `_deal_damage_to_target()` — 在挥砍中点触发伤害
- `can_pickup()` → bool
- `add_to_head_stack(item_data)` → 更新头顶精灵显示
- `remove_all_from_head()` → Array
- `_update_head_display()` → 刷新头顶堆叠精灵

修改现有逻辑：
- `_physics_process` 中的武器朝向：有攻击目标时朝目标，否则朝移动方向
- WeaponRange 信号连接 → 设置/清除 _attack_target
- PickupRange 信号连接 → 调用 dropped_item.attract_to(self)

### 10. `scenes/player.tscn`
新增子节点：
- WeaponRange (Area2D + CircleShape2D, radius~40)
- PickupRange (Area2D + CircleShape2D, radius~80)
- HeadStack (Node2D, position 在头顶上方)

### 11. `scenes/main.tscn`
- 放置 1-2 个 ResourceNode 实例
- 放置 1 个 CollectionZone 实例

## 武器挥砍动画（土豆兄弟风格）

```gdscript
func _start_swing(target_pos: Vector2) -> void:
    _is_swinging = true
    _swing_timer = 0.0

func _process_swing(delta: float) -> void:
    _swing_timer += delta
    var speed := _current_weapon.attack_speed if _current_weapon else 1.0
    var duration := 0.3 / speed
    if _swing_timer >= duration:
        $WeaponPivot/WeaponSprite.rotation = 0.0
        _is_swinging = false
        return
    var progress := _swing_timer / duration  # 0→1
    var arc := deg_to_rad(60.0)
    $WeaponPivot/WeaponSprite.rotation = arc * sin(progress * PI)
```

挥砍中点（progress ≈ 0.5）调用 `_deal_damage_to_target()`。

## 粒子特效配置

```gdscript
# 代码创建 GPUParticles2D 材质（像素碎石）
func _create_particle_material() -> ParticleProcessMaterial:
    var mat := ParticleProcessMaterial.new()
    mat.emission_shape = ParticleProcessMaterial.EMISSION_SHAPE_SPHERE
    mat.emission_sphere_radius = 8.0
    mat.direction = Vector3(0, -1, 0)
    mat.spread = 180.0
    mat.initial_velocity_min = 80.0
    mat.initial_velocity_max = 180.0
    mat.gravity = Vector3(0, 200, 0)
    mat.scale_min = 2.0
    mat.scale_max = 5.0
    mat.color = Color(0.6, 0.5, 0.4)  # 灰棕色矿石色
    mat.lifetime_randomness = 0.5
    return mat
```

## 教程结构

分两部分：
1. **第一部分：思路引导** — 架构推导、新概念讲解（GPUParticles2D、Area2D 自动索敌、sin 波形挥砍、吸附飞行）
2. **第二部分：完整操作步骤** — 每步含操作说明 + 完整代码 + 节点配置

## 验证清单

1. 玩家空手靠近矿石 → 不攻击
2. 装备武器靠近矿石 → 武器自动指向矿石并挥砍
3. 矿石 HP 归零 → 粒子爆射 + 矿石消失 + 物品散落
4. 掉落物品进入拾取范围 → 飞向玩家并堆叠在头顶
5. 堆叠满 10 个 → 不再吸附新物品
6. 玩家走到回收区 → 头顶物品清空 → stored 计数增加
7. 全程帧率稳定，无报错
