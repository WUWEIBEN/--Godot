# 挖呀挖 — Godot 项目指南

## 核心原则
- **始终优先使用 MCP 工具操作 Godot**，不要直接读写 `.tscn` 文件。`.gd` 脚本可通过 MCP 的 `script_manage` 编辑，当发现无法使用MCP时候先进行自行连接，实在需要直接修改文件需要询问用户。
- **`project.godot` 只能通过 MCP 工具或直接写文件修改**，编辑器不会热重载该文件，改完需用户手动 `Ctrl+Shift+P → reload`。

## 项目结构
- `scenes/` — 场景文件（.tscn）
- `scripts/` — GDScript 脚本（.gd）
- `addons/godot_mcp/` — Godot MCP 插件（DaxianLee/godot-mcp）
## UI 术语规则（强制）
- Godot 编辑器是**中文版**，任何时候提到编辑器内的按钮、面板、菜单、属性名，
  **必须英文和中文一起显示**，
- 例如：TileSet（图块集），Inspector（检查器），Atlas（图集），Terrain(地形)
- 不确定中文叫法时，先问我面板上显示的是什么，不要猜。

## MCP 连接
- 类型：HTTP → `http://127.0.0.1:3000/mcp`
- 服务器运行在 Godot 编辑器进程内，重载项目会短暂断连，无需重启 Claude Code


## plan mode 注意事项
- plan mode我有两种模式，一种是为我定制的，一种是为你工作定制的。在plan mode下请先判断意图再进行思考，如果判断模糊请先问我到底是要哪种。
- 为我定制的plan mode注重详细的操作流程步骤，主要是引导我亲自去完成开发工作。如果我需求有表达不清晰的地方请务必向我确认再规划。如果你有优秀的方案请告知我并对比我的方案给出优劣分析让我选择。
- 为你工作定制的则注重技术框架，技术细节不需要展示，你知道技术方案后可以自行通过MCP完成操作即可。
