# 教程三：掉落与拾取

**本教程目标**：矿石破坏后掉落资源物品，物品随机散开落地，玩家靠近后自动飞向玩家并堆叠在头顶。

完成后你可以：
- 矿石破坏时爆出若干个掉落物品
- 物品随机方向弹射 + 减速落地
- 玩家靠近时物品飞向玩家
- 物品堆叠显示在玩家头顶，满 10 个停止吸附

---

## 第一部分：思路引导

### 掉落物品的双重身份

DroppedItem 不是"抽象的游戏物品图标"，它就是**矿石被打碎后飞溅出去的真实碎片**。Scatter 弹射阶段同时承担了两件事：

1. **视觉**：碎片从矿石位置爆开、随机方向飞出、摩擦减速落地——这就是你要的飞溅效果
2. **游戏逻辑**：每个碎片携带 ItemData，落地后能被玩家吸附拾取

而 GPUParticles2D 退为配角——攻击命中的碎石渣（打击感）和死亡时的粉尘（过渡），真正吸引眼球的是那些飞出去的 DroppedItem。

### 掉落物品的生命周期

一个 DroppedItem 从出生到消失，经历哪些阶段？

<details>
<summary>想一想，再展开答案</summary>

```
1. spawn（出生）→ 设置位置、物品数据
2. scatter（弹射）→ 随机方向+速度飞出
3. 飞行 + 重力下拉 → 抛物线落地
4. 撞地反弹 → 弹 2-3 次，每次衰减
5. 静止 → 摩擦力减到几乎不动
6. 玩家靠近 → 被吸附，飞向玩家
7. 到达玩家 → 加入头顶堆栈，自身销毁
```

</details>

### 弹射物理怎么做？

不需要真正的物理引擎。在 `_physics_process` 里手动模拟四件事：

```
_velocity.y += GRAVITY * delta    # 重力不断往下拉

# 撞到地面时反弹
if global_position.y >= _landing_y:
    global_position.y = _landing_y       # 不穿过地面
    _velocity.y = -_velocity.y * BOUNCE # 弹起，速度衰减
    _velocity.x *= 0.85                  # 落地摩擦（只在撞地时生效）
```

**关键**：空中不加水平摩擦。真实抛物线的水平速度是恒定的，只有撞地反弹时才衰减。如果在空中每帧乘摩擦系数，水平速度半秒就没了，看起来就是原地起跳而非抛物线。

每个碎片在 scatter 时随机一个落地点 Y（矿石下方 20~50 像素），水平散开 ±0.3×force，所以碎片呈扇形向上散落。

吸附时不是匀速飞过去，而是**由慢到快加速**——用一个计时器从慢速渐变到满速：

```
var dir := (_target.global_position - global_position).normalized()
_pickup_elapsed += delta                         # 累计吸附时间
var progress := minf(_pickup_elapsed / 0.4, 1.0) # 0.4秒加速到满
var speed := lerpf(30.0, PICKUP_SPEED, ease(progress, -2.0))  # 30→400，先快后慢加速
_velocity = dir * speed
```

视觉上就是"慢悠悠开始 → 嗖地飞过来"。

### 碎片旋转怎么加？

每个碎片给一个角速度（弧度/秒），每帧驱动旋转：

```
_angular_velocity = abs(_velocity.y) * 0.05  # 飞得越高转得越快
_angular_velocity *= signf(randf_range(-1, 1)) # 随机方向

# 在 _physics_process 末尾
rotation += _angular_velocity * delta
_angular_velocity *= 0.97  # 旋转逐渐衰减
```

碎片弹出去的初速度越大（向上抛出越猛），自转越快。落地后旋转随衰减逐渐停下，看起来很自然。

### 头顶堆叠怎么显示？

玩家身上放一个 `HeadStack` 节点（Node2D），位置设在头顶。每拾取一个物品，就在 HeadStack 下动态创建一个 Sprite2D，往上叠：

```
第 1 个：偏移 (0, 0)
第 2 个：偏移 (0, -5)
第 3 个：偏移 (0, -10)
...
```

加上一点点随机微调，看起来更自然。

### 谁来驱动吸附？

两个 Area2D 各司其职：

| Area2D | 归属 | 检测什么 | 检测到后做什么 |
|--------|------|---------|---------------|
| WeaponRange (~40px) | 玩家 | "resources" 组的 ResourceNode | 玩家开始攻击 |
| PickupRange (~80px) | 玩家 | "dropped_items" 组的 DroppedItem | 调用 item.attract_to(玩家) |

DroppedItem 飞向玩家、判断到达、加入堆栈——这些逻辑都由 **DroppedItem 自己**处理，玩家的 PickupRange 只负责"通知你可以飞过来了"。

---

## 第二部分：完整操作步骤

### 步骤 1：创建 DroppedItem 脚本

新建 `scripts/dropped_item.gd`：

```gdscript
class_name DroppedItem
extends Area2D

signal picked_up(item_data)

const PICKUP_SPEED := 400.0
const GRAVITY := 300.0              # 像素/秒²，把碎片往下拉
const BOUNCE := 0.4                 # 撞地后保留 40% 速度
const PICKUP_DISTANCE := 10.0       # 距离玩家多远算"到达"

var item_data: ItemData
var _velocity: Vector2 = Vector2.ZERO       # 当前移动速度
var _target: Node2D = null                  # 吸附目标
var _attracted: bool = false                # 是否开始吸附
var _pickup_elapsed: float = 0.0            # 吸附持续时间
var _landing_y: float = 0.0                 # 落地点 Y
var _settled: bool = false                  # 是否已落地静止
var _angular_velocity: float = 0.0          # 旋转角速度（弧度/秒）

func scatter(origin: Vector2, force: float) -> void:
	global_position = origin
	_landing_y = origin.y + randf_range(20, 50)  # 落地点在矿石下方
	_velocity.x = randf_range(-force * 0.3, force * 0.3)   # 左右散开
	_velocity.y = -randf_range(force, force * 1.5)          # 向上抛出
	_settled = false
	# 旋转：飞得越高转得越快
	_angular_velocity = abs(_velocity.y) * 0.05
	_angular_velocity *= signf(randf_range(-1, 1))

func attract_to(target: Node2D) -> void:
	if not _settled:
		return
	_target = target
	_attracted = true
	_pickup_elapsed = 0.0

func _physics_process(delta: float) -> void:
	if _attracted and is_instance_valid(_target):
		# 由慢到快飞向目标
		_pickup_elapsed += delta
		var dir := _target.global_position - global_position
		var dist := dir.length()
		if dist < PICKUP_DISTANCE:
			if _target.has_method("add_to_head_stack"):
				if _target.add_to_head_stack(item_data):
					picked_up.emit(item_data)
					queue_free()
			return
		var speed_progress := minf(_pickup_elapsed / 0.4, 1.0)
		var current_speed := lerpf(30.0, PICKUP_SPEED, ease(speed_progress, -2.0))
		_velocity = dir.normalized() * current_speed
	else:
		# 重力（空中不加水平摩擦，保持抛物线）
		_velocity.y += GRAVITY * delta
		# 撞地反弹
		if global_position.y >= _landing_y:
			global_position.y = _landing_y
			_velocity.y = -_velocity.y * BOUNCE
			_velocity.x *= 0.85  # 落地摩擦
			if abs(_velocity.y) < 15.0:
				_velocity.y = 0.0
				_settled = true

	global_position += _velocity * delta
	rotation += _angular_velocity * delta
	_angular_velocity *= 0.97  # 旋转衰减

func _ready() -> void:
	# 给一点随机旋转，让物品看起来自然
	rotation = randf_range(0, TAU)
	
	# 用 item_data 的图标作为显示
	if item_data and item_data.icon:
		$Sprite2D.texture = item_data.icon
	else:
		# 未设置 icon 时用颜色占位
		var img := Image.create(16, 16, false, Image.FORMAT_RGBA8)
		img.fill(Color(0.55, 0.35, 0.15))
		$Sprite2D.texture = ImageTexture.create_from_image(img)
```

### 步骤 2：创建 DroppedItem 场景

新建 `scenes/dropped_item.tscn`：

```
DroppedItem (Area2D)              ← 挂载 dropped_item.gd 脚本
├── CollisionShape2D              ← CircleShape2D, radius=15（拾取检测圈）
└── Sprite2D                      ← scale=(0.5, 0.5)，缩小显示
```

**操作**：
1. 新建场景 → 根节点 Area2D，命名为 DroppedItem
2. 挂载脚本 `res://scripts/dropped_item.gd`
3. 添加子节点 CollisionShape2D（CircleShape2D, radius=15）
4. 添加子节点 Sprite2D，scale 设为 (0.5, 0.5)
5. 在 DroppedItem 的组（Groups）中添加 `"dropped_items"`

> Sprite2D 的 texture 不用管——脚本在 `_ready()` 里根据 item_data.icon 动态设置。

### 步骤 3：更新 resource_node.gd — 破坏时生成掉落物

修改 `scripts/resource_node.gd`，加上 `drop_scene` 导出变量和 `_spawn_drops()`：

```gdscript
class_name ResourceNode
extends StaticBody2D

@export var hp: int = 5
@export var max_hp: int = 5
@export var drop_count: int = 3
@export var drop_item_data: ItemData
@export var drop_scene: PackedScene  # ← 新增：掉落物场景
@export var texture_color: Color = Color(0.55, 0.35, 0.15)
@export var particle_color: Color = Color(0.55, 0.35, 0.15)

var _current_hp: int

func _ready() -> void:
	_current_hp = max_hp
	
	if $Sprite2D.texture == null:
		var img := Image.create(32, 32, false, Image.FORMAT_RGBA8)
		img.fill(texture_color)
		$Sprite2D.texture = ImageTexture.create_from_image(img)
	
	_particle_setup()

func _particle_setup() -> void:

	var mat := ParticleProcessMaterial.new()                              # 创建粒子材质（控制粒子怎么运动的"配方"）
	mat.emission_shape = ParticleProcessMaterial.EMISSION_SHAPE_SPHERE    # 粒子出生位置：球体内任意一点，不是从一个点喷出
	mat.emission_sphere_radius = 8.0                                               # 球体半径 8 像素，粒子从矿石中心周围一小块区域出现
	mat.direction = Vector3(0, -1, 0)                                     # 初始飞行方向（向上），spread 会让它散开而不是全往上
	mat.spread = 180.0                                                    # 扩散角度 180° = 朝所有方向飞（360° 的一半）
	mat.initial_velocity_min = 80.0                                       # 最小初速度（像素/秒），有的碎片飞得慢
	mat.initial_velocity_max = 180.0                                      # 最大初速度（像素/秒），有的飞得快，显得随机自然
	mat.gravity = Vector3(0, 200, 0)                                      # 重力（像素/秒²），碎片飞到一半往下掉
	mat.scale_min = 2.0                                                   # 粒子最小尺寸（像素），碎石有小块的
	mat.scale_max = 5.0                                                   # 粒子最大尺寸（像素），碎石有大块的
	mat.color = particle_color                                            # 粒子颜色，跟资源外观颜色一致（矿石用灰棕色）
	mat.lifetime_randomness = 0.5                                         # 存活时间随机度，0.5 意味着有的粒子先消失有的后消失
	$GPUParticles2D.process_material = mat                                # 把配好的材质赋值给 GPUParticles2D 节点

func take_damage(amount: int) -> void:
	_current_hp -= amount
	_spawn_hit_particles(amount)                    # 按伤害量迸发粒子
	_spawn_drops(1)                                 # 每次受伤掉 1 个碎片
	print(name, " HP: ", _current_hp, "/", max_hp)
	if _current_hp <= 0:
		_die()

func _spawn_hit_particles(damage: int) -> void:     # 受伤粒子（伤害越高粒子越多）
	$GPUParticles2D.amount = max(1, damage * 2)      # 每 1 点伤害 = 2 个粒子，最少 1 个
	var mat := $GPUParticles2D.process_material as ParticleProcessMaterial
	mat.initial_velocity_min = 20.0
	mat.initial_velocity_max = 20.0 + damage * 15.0
	$GPUParticles2D.restart()
	$GPUParticles2D.emitting = true

func _die() -> void:
	_spawn_death_dust()                               # 少量粉尘过渡（配角）
	$Sprite2D.visible = false
	$CollisionShape2D.set_deferred("disabled", true)
	_spawn_drops(drop_count)                          # 死亡时爆出剩余碎片
	get_tree().create_timer(0.8).timeout.connect(queue_free)

func _spawn_death_dust() -> void:                     # 粉尘粒子（少量，配角）
	$GPUParticles2D.amount = 8
	var mat := $GPUParticles2D.process_material as ParticleProcessMaterial
	mat.initial_velocity_min = 20.0
	mat.initial_velocity_max = 60.0
	$GPUParticles2D.restart()
	$GPUParticles2D.emitting = true

func _spawn_drops(count: int) -> void:
	if drop_scene == null or drop_item_data == null:
		return
	for i in count:
		var drop := drop_scene.instantiate() as DroppedItem
		drop.item_data = drop_item_data
		get_tree().current_scene.add_child(drop)
		drop.scatter(global_position, 180.0)       # 弹射力度 —— 碎片飞溅的主要视觉效果
```

保存后，打开 `scenes/ore_node.tscn`，在检查器中把 **Drop Scene** 指向 `res://scenes/dropped_item.tscn`。

### 步骤 4：给玩家添加 PickupRange 和 HeadStack

打开 `scenes/player.tscn`，添加两个新子节点：

| 节点 | 配置 |
|------|------|
| Area2D（命名 **PickupRange**） | collision_layer 和 mask 都勾选 Layer 2 |
| └── CollisionShape2D | CircleShape2D, radius = **80** |
| Node2D（命名 **HeadStack**） | position = **(0, -40)**（头顶上方） |

### 步骤 5：更新 player.gd — 头顶堆栈逻辑

在 `scripts/player.gd` 末尾追加以下代码：

```gdscript
# ========== 头顶堆栈 ==========

var head_stack: Array = []
var max_head_stack: int = 10

func can_pickup() -> bool:
	return head_stack.size() < max_head_stack

func add_to_head_stack(data: ItemData) -> bool:
	if not can_pickup():
		return false
	head_stack.append(data)
	_update_head_display()
	print("拾取矿石！当前堆叠: ", head_stack.size(), "/", max_head_stack)
	return true

func remove_all_from_head() -> Array:
	var items := head_stack.duplicate()
	head_stack.clear()
	_update_head_display()
	return items

func _update_head_display() -> void:
	# 清除旧显示
	for child in $HeadStack.get_children():
		child.queue_free()
	
	# 重新生成堆叠精灵
	for i in head_stack.size():
		var sprite := Sprite2D.new()
		var data := head_stack[i] as ItemData
		if data and data.icon:
			sprite.texture = data.icon
		else:
			var img := Image.create(16, 16, false, Image.FORMAT_RGBA8)
			img.fill(Color(0.55, 0.35, 0.15))
			sprite.texture = ImageTexture.create_from_image(img)
		sprite.scale = Vector2(0.4, 0.4)
		sprite.position = Vector2(randf_range(-3, 3), -i * 5.0)
		$HeadStack.add_child(sprite)
```

同时，在 `_ready()` 末尾添加 PickupRange 信号连接：

```gdscript
	# 连接拾取范围信号
	$PickupRange.area_entered.connect(_on_pickup_range_entered)
```

然后添加回调方法：

```gdscript
# ========== 拾取范围检测 ==========

func _on_pickup_range_entered(area: Area2D) -> void:
	if body.is_in_group("dropped_items") and body is DroppedItem:
		if can_pickup():
			body.attract_to(self)
```

---

## 验证

1. 运行游戏，装备武器，靠近矿石
2. 矿石破坏 → 矿石图标碎片从矿石位置爆射出来（这是主要飞溅效果）+ 少量粉尘辅助
3. 碎片散开、逐渐减速停在地面
4. 玩家靠近掉落物 → 物品飞向玩家
5. 物品到达 → 出现在玩家头顶，一个个往上叠
6. 控制台输出：`拾取矿石！当前堆叠: 1/10`
7. 堆叠满 10 个 → 新掉落物不再飞向玩家（停在地上不动）
